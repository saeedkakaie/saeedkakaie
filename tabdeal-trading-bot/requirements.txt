# نسخه‌ها عمداً دقیق (نه >=) پین شده‌اند: رفتار ربات (به‌خصوص پارس کردن
# پاسخ سفارش‌ها در src/exchange_client.py) بر اساس رفتار مشاهده‌شده‌ی
# همین نسخه از tabdeal-python کالیبره شده. آپدیت این پکیج بدون تست دوباره
# می‌تواند دوباره باگ‌های مشابه قبلی (ساختار پاسخ متفاوت) ایجاد کند.
tabdeal-python==0.4.7
python-dotenv==1.2.2
Flask==3.1.3
